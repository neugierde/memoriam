<%= form_with(model: taxon) do |form| %>
  <% if taxon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(taxon.errors.count, "error") %> prohibited this taxon from being saved:</h2>

      <ul>
        <% taxon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ancestry %>
    <%= form.number_field :ancestry %>
  </div>

  <div class="field">
    <%= form.label :taxonomy_id %>
    <%= form.text_field :taxonomy_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
